
const text=["lolol","rofl","omg","tams" ];

const caps=text.map(function(ele){
    let x=ele.toUpperCase()
    return x;
})

console.log(text);
console.log(caps);


// Map creates a new array with a result of calling a callback on every element in the array
